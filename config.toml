# Project-local overrides for editable installs from the thesis repo.
# Any non-[paths] settings here override the packaged defaults in
# src/glmhmmt/resources/default_config.toml.

[paths]
data_dir = "../data"
results_dir = "../code/results"
alexis_dir = "../data/Alexis"

[plugins]
task_paths = ["../bcb-behavioral-tasks/tasks"]
